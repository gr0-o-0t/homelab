# ─────────────────────────────────────────────────────────────────────────────
# Template: caddy/conf.d/_template.conf
#
# Copy this file to services/<your-service>/caddy.conf and adjust:
#   • The hostname pattern
#   • The upstream target (container name : port)
#   • Any extra directives (headers, auth, etc.)
#
# Enable with: make enable SERVICE=<your-service>
# ─────────────────────────────────────────────────────────────────────────────

# <service>.{$HOME_SUBDOMAIN}.{$DOMAIN} {
#     import wildcard_tls
#
#     # Proxy to the service container.  Caddy resolves the container name
#     # via Docker DNS on the home-services network.
#     reverse_proxy <container-name>:<port>
#
#     # Optional: enforce that requests come only from your tailnet.
#     # Tailscale already gates access at the network layer, but this adds
#     # defence-in-depth at the HTTP layer.
#     @not_tailnet {
#         not remote_ip 100.64.0.0/10
#     }
#     respond @not_tailnet 403
# }
