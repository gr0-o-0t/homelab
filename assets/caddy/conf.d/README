# This directory is intentionally kept in git (via .gitkeep) but its *.conf
# contents are generated by `scripts/enable-service.sh`.
#
# To expose a service:
#   make enable SERVICE=immich
#
# To disable a service:
#   make disable SERVICE=immich
