http://ollama.{$DOMAIN} {


    # Ollama responses can be large and streamed; flush_interval -1 disables
    # response buffering so tokens are delivered to the client immediately.
    reverse_proxy ollama:11434 {
        flush_interval -1
    }
}
