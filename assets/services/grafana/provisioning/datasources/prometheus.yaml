# Auto-provisions the shared homelab Prometheus as Grafana's default datasource.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://homelab-prometheus:9090
    isDefault: true
    editable: false
