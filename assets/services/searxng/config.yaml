vars: {}
secrets:
  SECRET_KEY:
    required: true
    description: "Signs search request state - any long random string"
databases:
  - redis:
      env:
        dsn: SEARXNG_REDIS_URL
ports:
  - 8080