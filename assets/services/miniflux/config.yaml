vars:
  ADMIN_USERNAME:
    value: "admin"
    required: false

secrets:
  ADMIN_PASSWORD:
    required: true

databases:
  postgres:
    database: miniflux
    user: miniflux
    env:
      "postgres://{user}:{password}@{host}:{port}/{database}?sslmode=disable": DATABASE_URL
