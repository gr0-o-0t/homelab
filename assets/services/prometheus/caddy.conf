# Caddy site block for Prometheus (private / tailnet).
# Enabled by: homelab enable prometheus

prometheus.{$HOME_SUBDOMAIN}.{$DOMAIN} {
    import wildcard_tls

    reverse_proxy homelab-prometheus:9090
}
