# Caddy site block for NetBox (private / tailnet).
# Enabled by: homelab service enable netbox --private

netbox.{$HOME_SUBDOMAIN}.{$DOMAIN} {
    import wildcard_tls

    reverse_proxy netbox:8080
}
