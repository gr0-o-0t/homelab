# Caddy site block for NetBox (public / Cloudflare Tunnel).
# Enabled by: homelab service enable netbox --public

netbox.{$PUB_SUBDOMAIN}.{$DOMAIN} {
    import wildcard_tls

    reverse_proxy netbox:8080
}
